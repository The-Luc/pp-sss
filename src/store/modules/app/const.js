export const MODULE_NAME = "app";

const _GETTERS = {};

const _ACTIONS = {};

export const ACTIONS = {};

const _MUTATES = {
  HANDLEISDIALOG: "handleIsDialog"
};

export const MUTATES = {
  HANDLEISDIALOG: `${MODULE_NAME}/${_MUTATES.HANDLEISDIALOG}`
};

export default {
  _GETTERS,
  _ACTIONS,
  _MUTATES
};
